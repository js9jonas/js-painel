[phases.setup]
nixPkgs = ['nodejs_20', 'python311', 'python311Packages.pip']

[phases.install]
cmds = [
  'npm ci --legacy-peer-deps',
  'python3 -m venv /app/venv',
  '/app/venv/bin/pip install curl-cffi',
]
